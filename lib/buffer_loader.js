class BufferLoader {
  constructor(audioCtx, soundUrls, fn) {
    this.audioCtx = audioCtx;
    this.soundUrls = soundUrls;
    this.index = 0;
  }
}

  // a function
  this.functionFinished = functionFinishedPm;
  // array of buffers which I create
  this.arrBuffer = new Array();
  // which file are we on
  this.ind = 0;
}
